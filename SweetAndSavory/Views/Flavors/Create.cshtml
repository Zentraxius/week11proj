@{
  Layout = "_Layout";
}

@model SweetAndSavory.Models.Flavor

<h3>Add Flavor</h3>

@using (Html.BeginForm())
{
    @Html.LabelFor(model => model.Name)
    @Html.TextBoxFor(model => model.Name)
    <input type="submit" value="Create Flavor" class="btn"/>
}